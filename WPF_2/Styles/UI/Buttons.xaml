<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="OriginalButton" TargetType="Button">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Height" Value="25"/>
        <Setter Property="Background" Value="{DynamicResource BackGroundButton}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                        <ContentPresenter 
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource MouseButton}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ButtonPressedBackground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        <Setter Property="Background" Value="{DynamicResource DangerBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource DangerBrush}"/>
        <Setter Property="Width" Value="130"/>
        <Setter Property="Content" Value="🗑️ Удалить"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource DangerHoverBrush}"/>
                
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource DangerPressedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ChangeTheme" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        <Setter Property="Width" Value="130"/>
    
        <Setter Property="Margin" Value="0,5,0,5"/>
    </Style>

    <Style x:Key="BackButton4" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        <Setter Property="Width" Value="130"/>
       
    </Style>

    <Style x:Key="StartPriemButton" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        <Setter Property="Width" Value="130"/>
        
    </Style>

    <Style x:Key="AddButton" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        <Setter Property="Width" Value="130"/>
       
    </Style>

    <Style x:Key="EditPacientButton" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        <Setter Property="Height" Value="25"/>
        
    </Style>

    <Style x:Key="SettingsButton" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        <Setter Property="Width" Value="120"/>
        <Setter Property="Margin" Value="5"/>
       
    </Style>

    <Style x:Key="SignInButton" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        <Setter Property="Width" Value="150"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="85,0,0,0"/>
       
    </Style>

    <Style x:Key="SaveButton" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        <Setter Property="Width" Value="120"/>
        <Setter Property="Margin" Value="5"/>
        
    </Style>

    <Style x:Key="BackButton" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        <Setter Property="Width" Value="80"/>
        <Setter Property="Margin" Value="5"/>
        
    </Style>

    <Style x:Key="EditButton" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        <Setter Property="Width" Value="150"/>
        <Setter Property="Margin" Value="5"/>
        
    </Style>

    <Style x:Key="RegistrationButton" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
      
        <Setter Property="Width" Value="150"/>
    </Style>

    <Style x:Key="SignInButton2" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        <Setter Property="Width" Value="150"/>
        <Setter Property="Margin" Value="0,5,0,0"/>
        
    </Style>

    <Style x:Key="SaveEditButton" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
       
        <Setter Property="Width" Value="150"/>
    </Style>

    <Style x:Key="BAckButton2" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
       
        <Setter Property="Width" Value="150"/>
        <Setter Property="Margin" Value="0,5,0,0"/>
    </Style>

    <Style x:Key="AddPAcientButton" TargetType="Button" BasedOn="{StaticResource OriginalButton}">
        
        <Setter Property="Width" Value="150"/>
    </Style>

    <Style x:Key="BAckButton3" TargetType="Button" BasedOn="{StaticResource OriginalButton}">

        <Setter Property="Width" Value="150"/>
        <Setter Property="Margin" Value="0,5,0,0"/>
    </Style>

</ResourceDictionary>