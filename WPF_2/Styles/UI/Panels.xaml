<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="StackPanel">
        <Setter Property="Height" Value="100"/>
    </Style>

    <Style x:Key="MenuBackground" TargetType="Menu">
        <Setter Property="Background" Value="{DynamicResource Background}"/>
    </Style>
    
    <Style x:Key="InfoWrapPanel" TargetType="WrapPanel">
        <Setter Property="Height" Value="50"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="WrapPanelButtons" TargetType="WrapPanel">
        <Setter Property="Height" Value="40"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin" Value="10"/>
    </Style>

    <Style x:Key="InfoStackPanel" TargetType="StackPanel">
        <Setter Property="Height" Value="160"/>
    </Style>

    <Style x:Key="DoctorStackPanelTextBox" TargetType="StackPanel">
        <Setter Property="Height" Value="300"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="DoctorStackPanelLabel" TargetType="StackPanel">
        <Setter Property="Height" Value="300"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="PacientStackPanelTextBox" TargetType="StackPanel">
        <Setter Property="Height" Value="300"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="PacientStackPanelLabel" TargetType="StackPanel">
        <Setter Property="Height" Value="300"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="WrapPanelSignIn" TargetType="WrapPanel">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="WrapPanelPriem" TargetType="WrapPanel">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Margin" Value="0,10"/>
    </Style>

    <Style x:Key="WrapPanelTextBlock" TargetType="WrapPanel">
        <Setter Property="Margin" Value="0,2,0,0"/>
    </Style>

    <Style x:Key="StackPanelDiagnosis" TargetType="StackPanel">
        <Setter Property="Margin" Value="0,10,0,0"/>
        <Setter Property="Orientation" Value="Vertical"/>
    </Style>

    <Style x:Key="StackPanelRecomendations" TargetType="StackPanel">
        <Setter Property="Margin" Value="0,10,0,0"/>
        <Setter Property="Orientation" Value="Vertical"/>
    </Style>

    <Style x:Key="WrapPanelWithButtons" TargetType="WrapPanel">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,20"/>
    </Style>

</ResourceDictionary>